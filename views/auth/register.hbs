<h1>Register</h1>
{{#if messages}}
    <p class="alert alert-danger">We could not complete your Registration</p>        
{{else}}
    <p class="alert alert-secondary">Please create your account</p>   
{{/if}}
<form method="post">
    <fieldset class="form-group">
        <label for="username" class="col-2 control-label">Username: *</label>
        <input name="username" id="username" required type="email" placeholder="email@email.com" />
    </fieldset>
    <fieldset class="form-group">
        <label for="password" class="col-2 control-label">Password: *</label>
        <input name="password" id="password" type="password" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" />
    </fieldset>
    <fieldset class="form-group">
        <label for="confirm" class="col-2 control-label">Confirm: *</label>
        <input name="confirm" id="confirm" type="password" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,}" />
    </fieldset>
    <fieldset class="form-group">
        <button class="btn btn-secondary offset-2">Register</button>
        <hr />
        Already have an account? <a href="/auth/login" title="Login">Login</a>
    </fieldset>
</form>